package com.chatbotProject.chatbot.config;

/*
 * import org.springframework.context.annotation.Configuration; import
 * org.springframework.http.HttpMethod; import
 * org.springframework.security.config.annotation.authentication.builders.
 * AuthenticationManagerBuilder; import
 * org.springframework.security.config.annotation.web.builders.HttpSecurity;
 * import
 * org.springframework.security.config.annotation.web.builders.WebSecurity;
 * import org.springframework.security.config.annotation.web.configuration.
 * WebSecurityConfigurerAdapter;
 * 
 * 
 * @Configuration public class SecurityConfig extends
 * WebSecurityConfigurerAdapter { // Protecting the urls with a role-based
 * access.
 * 
 * @Override protected void configure(HttpSecurity http) throws Exception {
 * http.csrf().disable().authorizeRequests() .anyRequest().authenticated()
 * .and() .httpBasic(); }
 * 
 * @Override protected void configure(AuthenticationManagerBuilder auth) throws
 * Exception { auth.inMemoryAuthentication().withUser("htd(llft@%$#").password(
 * "{noop}hyst@#$%kjv").roles("USER");
 * auth.inMemoryAuthentication().withUser("admin").password("{noop}supersecret")
 * .roles("ADMIN"); }
 * 
 * }
 */